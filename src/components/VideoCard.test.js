import { fireEvent } from '@testing-library/dom';
import { render } from '@testing-library/react';
import VideoCard from './VideoCard';

describe('Video Card', ()=> {
    test('Video card not exist on page load', () => {

    })
})